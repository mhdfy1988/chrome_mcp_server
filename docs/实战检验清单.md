# 实战检验 TODO 清单

这份清单用来跟踪 `chrome_mcp_server` 的真实网页自动化检验进度。

目标不是只验证“某个工具能调用”，而是验证整条链路是否稳定：

- 页面理解是否准确
- 目标定位是否跑偏
- 动作执行后是否真的发生了预期变化
- 结果验证是否只看 `URL`，还是能看到内容就绪
- 出现新页、弹窗、局部刷新时，返回页是否正确

## 通过标准

每个任务默认至少满足这几条，才算真正通过：

1. 有明确的入口、动作和成功信号
2. 至少保留一张关键步骤截图
3. 失败时能回溯到是“页面理解 / 目标定位 / 动作执行 / 结果验证”哪一层出了问题
4. 搜索或跳转类任务，不能只看 `URL` 变化，必须看到内容区或详情页真的出来
5. 视频类任务，不能只点开详情页，必须验证进入播放态

## 当前进度

- 已完成：`13 / 15`
- 待继续：`2 / 15`

## 已完成

- [x] `Wikipedia 搜索词条`
  覆盖：首页搜索、词条跳转、本地化分支。
  加测：点击目录锚点并提取对应小节正文。

- [x] `GitHub 仓库页导航`
  覆盖：仓库标签切换、同页导航、`URL` 变化。
  加测：打开一个源码文件并提取代码片段。

- [x] `Bilibili 首页搜索`
  覆盖：主输入框识别、搜索提交、新页/新 `target` 结果。
  加测：结果页进入视频详情并验证播放态。
  实测补充：首页存在 banner 级遮挡层，自动关闭未消除，但不影响输入；搜索触发新页 `popup`；详情页 `video` 已自动播放（`read_media_state` 验证）。

- [x] `Unsplash 图片搜索与详情`
  覆盖：图片搜索、结果页、图片详情页、两阶段等待。
  加测：进入详情后切换相关图片或下一张。

- [x] `TED 演讲详情阅读`
  覆盖：列表进入详情、正文提取、摘要输出。
  加测：打开讲者信息或相关演讲链接。

- [x] `MDN 文档阅读`
  覆盖：文章页打开、结构快照、正文提取。
  加测：点击页内目录锚点并提取对应小节。

- [x] `Hacker News 榜单抓取`
  覆盖：首页列表抓取、前 10 条汇总、Markdown 输出。
  加测：打开首条外链后返回列表页继续操作。

- [x] `Wikipedia 表格提取`
  覆盖：表格页打开、前 5 行提取、Markdown 输出。
  加测：切换到另一个表格并再次提取。

- [x] `Vimeo 搜索 + 详情 + 播放`
  覆盖：首页理解 `Watch` 入口、进入 `Watch` 页、搜索、打开视频详情、点击播放并验证进入播放态。
  加测：切换字幕或全屏按钮可见性验证。

- [x] `Python Docs 文档阅读`
  覆盖：文档页打开、结构快照、正文提取、摘要输出。
  加测：使用页内搜索框或点击目录锚点。

- [x] `YouTube 搜索 + 详情 + 播放`
  覆盖：首页搜索框识别、`Enter` 提交搜索、结果页首条视频标题链接进入详情、自动播放态验证、字幕/全屏控件存在验证。
  加测：广告/预热视频场景下仍能验证播放态。

- [x] `Notion Help / 帮助中心搜索`
  覆盖：帮助中心首页搜索框识别、`Enter` 提交搜索、结果页内容就绪验证、进入帮助文章页、正文提取与摘要输出。
  加测：进入相关文章或下一条结果并提取正文。

- [x] `公开电商页搜索 + 商品详情`
  覆盖：`eBay` 首页搜索框识别、`Enter` 提交搜索、结果页商品标题链接绑定、新页商品详情打开、标题/价格/成色/配送/卖家字段提取与摘要输出。
  加测：切换不同排序或筛选并再次进入详情页。

## 待继续

- [ ] `Stack Overflow 搜索 + 问答摘要`
  目标：搜索问题、进入首条问答、提取题目和高票回答摘要。
  当前结果：入口被 `Cloudflare` 安全验证拦截，暂未形成稳定的无人值守链路；证据已留存在 `.tmp/live-runs`。
  加测：若入口放行，补做代码块复制或答案摘要二次校验。

- [ ] `Product Hunt / 公开产品列表页`
  目标：进入榜单、打开首条产品详情、提取核心字段并生成简报。
  当前结果：入口被 `Cloudflare` 安全验证拦截，暂未形成稳定的无人值守链路；证据已留存在 `.tmp/live-runs`。
  加测：若入口放行，补做评论区或外链跳转验证。

## 业务模板待落地

- [ ] `登录 / 登出模板`
  适合后续接你的低风险测试账号页面。

- [ ] `筛选 / Tab / 局部刷新模板`
  适合验证同页刷新、列表更新、筛选器变化。

- [ ] `新页 / 新标签模板`
  适合验证点击后打开新页、活动页切换、回到原页继续操作。

- [ ] `抓取并汇总模板`
  适合公告页、榜单页、资料目录页的数据提取与 Markdown 输出。

## 下一轮建议顺序

1. `Stack Overflow`
2. `Product Hunt`

## 使用方式

后面每跑完一项，就做 3 件事：

1. 把对应任务改成 `[x]`
2. 在任务后面补一句“这轮实际覆盖了什么”
3. 如有必要，把稳定经验沉淀回 `tests/web-tasks/*.json` 或 `README.md`
